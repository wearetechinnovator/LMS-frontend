import React, { useState, useMemo } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import ExportButton from '../../components/ExportButton'

// Initial Pre-populated campaigns data with added traffic & revenue fields for funnel
const initialCampaigns = [
  {
    id: 'CMP-001',
    name: 'Google Ads - B.Tech Admissions 2026',
    type: 'Google Ads',
    source: 'Google Ads API',
    status: 'ACTIVE',
    startDate: '2026-04-01',
    endDate: '2026-07-31',
    budget: 15000,
    impressions: 120000,
    clicks: 8400,
    leads: 840,
    qualifiedLeads: 620,
    convertedLeads: 120,
    revenue: 180000,
    owner: 'John Doe (Self)',
    description: 'Targeted keyword search campaign for prospective Engineering students.',
    mapping: 'Website Form & Landing Page',
    assignment: 'Round Robin Distribution',
    utmSource: 'google',
    utmMedium: 'cpc',
    utmName: 'btech_admissions_2026',
    routingTeam: 'Sales Team A',
    routingRule: 'Round Robin'
  },
  {
    id: 'CMP-002',
    name: 'Meta Ads - MBA Lookalike Inquiries',
    type: 'Facebook Ads',
    source: 'Facebook Lead Form',
    status: 'ACTIVE',
    startDate: '2026-04-05',
    endDate: '2026-06-30',
    budget: 12000,
    impressions: 95000,
    clicks: 7100,
    leads: 710,
    qualifiedLeads: 490,
    convertedLeads: 85,
    revenue: 127500,
    owner: 'Jane Smith',
    description: 'Lookalike audience advertising focusing on corporate managers.',
    mapping: 'Facebook API',
    assignment: 'Auto Assign to Sales Team A',
    utmSource: 'facebook',
    utmMedium: 'social_ads',
    utmName: 'mba_lookalike_2026',
    routingTeam: 'Sales Team A',
    routingRule: 'Auto Assignment Rules'
  },
  {
    id: 'CMP-003',
    name: 'WhatsApp - Q3 Executive MBA Outreach',
    type: 'WhatsApp Campaign',
    source: 'API Integration',
    status: 'PAUSED',
    startDate: '2026-04-10',
    endDate: '2026-05-30',
    budget: 3500,
    impressions: 48000,
    clicks: 3200,
    leads: 320,
    qualifiedLeads: 210,
    convertedLeads: 45,
    revenue: 67500,
    owner: 'John Doe (Self)',
    description: 'Bulk chat messaging funnel pointing to enrollment calendars.',
    mapping: 'API Webhook',
    assignment: 'Manual Assignment',
    utmSource: 'whatsapp',
    utmMedium: 'chat',
    utmName: 'q3_emba_outreach',
    routingTeam: 'Sales Team B',
    routingRule: 'Manual Assignment'
  },
  {
    id: 'CMP-004',
    name: 'Email - Scholarship Admissions Drive',
    type: 'Email Marketing',
    source: 'Landing Page',
    status: 'ACTIVE',
    startDate: '2026-04-12',
    endDate: '2026-08-31',
    budget: 2500,
    impressions: 150000,
    clicks: 11000,
    leads: 1100,
    qualifiedLeads: 820,
    convertedLeads: 195,
    revenue: 292500,
    owner: 'Jane Smith',
    description: 'Weekly scholarship newsletters targeting alumni list dropouts.',
    mapping: 'Website Form',
    assignment: 'Auto Assign to Sales Team B',
    utmSource: 'newsletter',
    utmMedium: 'email',
    utmName: 'scholarship_drive_2026',
    routingTeam: 'Sales Team B',
    routingRule: 'Auto Assignment Rules'
  },
  {
    id: 'CMP-005',
    name: 'Webinar - Virtual Campus Fair Outreach',
    type: 'Webinar',
    source: 'Landing Page',
    status: 'COMPLETED',
    startDate: '2026-03-01',
    endDate: '2026-03-25',
    budget: 4500,
    impressions: 30000,
    clicks: 4800,
    leads: 480,
    qualifiedLeads: 310,
    convertedLeads: 58,
    revenue: 87000,
    owner: 'Alex Johnson',
    description: 'Admissions open house virtual webinar tracking.',
    mapping: 'Landing Page Integration',
    assignment: 'Round Robin Distribution',
    utmSource: 'webinar',
    utmMedium: 'video',
    utmName: 'virtual_fair_2026',
    routingTeam: 'Counselors Pool',
    routingRule: 'Round Robin'
  }
]

// Mock leads list generated by campaigns for details view
const initialLeadsList = [
  { id: 'LD-101', name: 'Alice Cooper', email: 'alice@example.com', status: 'Converted', source: 'Google Ads', campaignId: 'CMP-001', assignedTo: 'Sales Team A', score: 95, lastActivity: 'Email Opened - 2h ago' },
  { id: 'LD-102', name: 'Bob Marley', email: 'bob@example.com', status: 'Qualified', source: 'Google Ads', campaignId: 'CMP-001', assignedTo: 'Sales Team A', score: 80, lastActivity: 'Call Answered - 1d ago' },
  { id: 'LD-103', name: 'Charlie Puth', email: 'charlie@example.com', status: 'Contacted', source: 'Google Ads', campaignId: 'CMP-001', assignedTo: 'Sales Team B', score: 65, lastActivity: 'WhatsApp Sent - 5h ago' },
  { id: 'LD-104', name: 'Diana Ross', email: 'diana@example.com', status: 'New', source: 'Facebook Lead Form', campaignId: 'CMP-002', assignedTo: 'Sales Team B', score: 45, lastActivity: 'Form Submitted - 30m ago' },
  { id: 'LD-105', name: 'Elvis Presley', email: 'elvis@example.com', status: 'Converted', source: 'Facebook Lead Form', campaignId: 'CMP-002', assignedTo: 'Sales Team A', score: 90, lastActivity: 'Meeting Completed - 1d ago' },
  { id: 'LD-106', name: 'Fiona Apple', email: 'fiona@example.com', status: 'Lost', source: 'API Integration', campaignId: 'CMP-003', assignedTo: 'Unassigned', score: 20, lastActivity: 'No Response - 3d ago' },
  { id: 'LD-107', name: 'George Harrison', email: 'george@example.com', status: 'Qualified', source: 'Landing Page', campaignId: 'CMP-004', assignedTo: 'Sales Team B', score: 75, lastActivity: 'Link Clicked - 4h ago' },
  { id: 'LD-108', name: 'Jimi Hendrix', email: 'jimi@example.com', status: 'Converted', source: 'Landing Page', campaignId: 'CMP-004', assignedTo: 'Sales Team B', score: 95, lastActivity: 'Paid Enrollment - 1d ago' },
  { id: 'LD-109', name: 'Janis Joplin', email: 'janis@example.com', status: 'New', source: 'Landing Page', campaignId: 'CMP-005', assignedTo: 'Round Robin Distribution', score: 50, lastActivity: 'Registered - Webinar' }
]

// Mock activities log for campaign details
const initialActivities = [
  { id: 1, campaignId: 'CMP-001', date: '2026-05-20', time: '11:42', type: 'Budget Updated', user: 'John Doe', text: 'Daily Google Ads budget increased by $50 to support summer keywords.' },
  { id: 2, campaignId: 'CMP-001', date: '2026-05-18', time: '09:15', type: 'Tracking Setup', user: 'Jane Smith', text: 'Google Analytics tracking verified. Goal conversions sync perfectly.' },
  { id: 3, campaignId: 'CMP-001', date: '2026-04-01', time: '08:00', type: 'Campaign Created', user: 'System', text: 'Campaign initialized with Round Robin Assignment Rules.' },
  { id: 4, campaignId: 'CMP-002', date: '2026-05-15', time: '14:30', type: 'Budget Updated', user: 'Jane Smith', text: 'Ad creative carousel refreshed with new testimonial photos.' },
  { id: 5, campaignId: 'CMP-002', date: '2026-04-05', time: '09:00', type: 'Campaign Created', user: 'System', text: 'Meta campaign launched targeting Lookalike MBA inquiries.' },
  { id: 6, campaignId: 'CMP-003', date: '2026-05-10', time: '10:00', type: 'Campaign Paused', user: 'John Doe', text: 'Campaign paused due to budget allocation changes.' }
]

export default function Campaigns() {
  const role = localStorage.getItem('userRole') || 'vendor'
  const isMasked = role === 'counselor' || role === 'vendor'
  const maskEmail = (email) => {
    if (!email) return ''
    const atIdx = email.indexOf('@')
    if (atIdx <= 0) return email
    const local = email.substring(0, atIdx)
    const domain = email.substring(atIdx)
    return local.charAt(0) + '***' + domain
  }

  // Active navigation tab: 'DASHBOARD' | 'CAMPAIGNS' | 'REPORTS' | 'CREATE_CAMPAIGN' | 'DETAILS'
  const [activeTab, setActiveTab] = useState('DASHBOARD')
  const [prevTab, setPrevTab] = useState('DASHBOARD')

  // Campaigns list state
  const [campaigns, setCampaigns] = useState(initialCampaigns)
  const [selectedCampaignId, setSelectedCampaignId] = useState('CMP-001')

  // Details Tab Sub-View: 'OVERVIEW' | 'LEADS' | 'ACTIVITIES' | 'ANALYTICS' | 'SETTINGS'
  const [detailsSubTab, setDetailsSubTab] = useState('OVERVIEW')

  // Filter States (All Campaigns tab)
  const [filterType, setFilterType] = useState('ALL')
  const [filterStatus, setFilterStatus] = useState('ALL')
  const [searchTerm, setSearchTerm] = useState('')

  // Create Campaign Tracking URL state
  const [generatedUrlVal, setGeneratedUrlVal] = useState('https://lms.edu/enroll')
  const [showAdvancedSettings, setShowAdvancedSettings] = useState(false)
  const [newCampaign, setNewCampaign] = useState({
    name: '',
    description: '',
    type: '',
    owner: 'John Doe (Self)',
    budget: '',
    startDate: new Date().toISOString().split('T')[0],
    endDate: '',
    sources: [], // Landing Page, Form, WhatsApp, Phone Calls, Manual Import
    utmSource: '',
    utmMedium: '',
    utmName: '',
    routingTeam: 'Sales Team A',
    routingRule: 'Round Robin',
    notes: '',
    customParams: ''
  })

  // Collapsible tracking url configuration
  const [showAdvancedTracking, setShowAdvancedTracking] = useState(false)

  // Custom campaign name modal (for step 1)
  const [showCreateNameModal, setShowCreateNameModal] = useState(false)
  const [newCustomName, setNewCustomName] = useState('')
  const [customCampaignNames, setCustomCampaignNames] = useState([])

  // Action Menu dropdown visibility state
  const [activeActionMenuId, setActiveActionMenuId] = useState(null)

  // Edit campaign modal state
  const [editingCampaign, setEditingCampaign] = useState(null)

  // Activities logger state
  const [activities, setActivities] = useState(initialActivities)
  const [newNote, setNewNote] = useState('')

  // Toast message
  const [toastMsg, setToastMsg] = useState(null)

  const triggerToast = (msg) => {
    setToastMsg(msg)
    setTimeout(() => setToastMsg(null), 3000)
  }

  // Active Campaign Object
  const activeCampaign = useMemo(() => {
    return campaigns.find(c => c.id === selectedCampaignId) || campaigns[0]
  }, [campaigns, selectedCampaignId])

  // Calculated Aggregate Stats
  const aggregateStats = useMemo(() => {
    const total = campaigns.length
    const active = campaigns.filter(c => c.status === 'ACTIVE').length
    const leads = campaigns.reduce((sum, c) => sum + c.leads, 0)
    const qualified = campaigns.reduce((sum, c) => sum + c.qualifiedLeads, 0)
    const converted = campaigns.reduce((sum, c) => sum + c.convertedLeads, 0)
    const budget = campaigns.reduce((sum, c) => sum + c.budget, 0)
    const revenue = campaigns.reduce((sum, c) => sum + (c.revenue || c.convertedLeads * 1500), 0)

    const overallRoi = budget > 0 ? ((revenue - budget) / budget) * 100 : 0
    const cpl = leads > 0 ? budget / leads : 0

    return { total, active, leads, qualified, converted, budget, revenue, overallRoi, cpl }
  }, [campaigns])

  // All Campaigns Filtering Logic
  const filteredCampaigns = useMemo(() => {
    return campaigns.filter(c => {
      const matchesSearch = c.name.toLowerCase().includes(searchTerm.toLowerCase()) || c.owner.toLowerCase().includes(searchTerm.toLowerCase())
      const matchesType = filterType === 'ALL' || c.type === filterType
      const matchesStatus = filterStatus === 'ALL' || c.status === filterStatus
      return matchesSearch && matchesType && matchesStatus
    })
  }, [campaigns, searchTerm, filterType, filterStatus])

  // Generated UTM URL preview
  const generatedUrl = useMemo(() => {
    const baseUrl = 'https://'
    const params = new URLSearchParams()
    if (newCampaign.utmSource) params.append('utm_source', newCampaign.utmSource.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.utmMedium) params.append('utm_medium', newCampaign.utmMedium.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.utmName) params.append('utm_campaign', newCampaign.utmName.toLowerCase().replace(/\s+/g, '_'))
    else if (newCampaign.name) params.append('utm_campaign', newCampaign.name.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.type) params.append('utm_content', newCampaign.type.toLowerCase().replace(/\s+/g, '_'))
    const queryString = params.toString()
    return queryString ? `${baseUrl}?${queryString}` : baseUrl
  }, [newCampaign])

  // Custom campaign name selection handler
  const handleAddCustomName = (e) => {
    e.preventDefault()
    if (newCustomName.trim()) {
      setCustomCampaignNames([...customCampaignNames, newCustomName.trim()])
      setNewCampaign(prev => ({ ...prev, name: newCustomName.trim() }))
      setNewCustomName('')
      setShowCreateNameModal(false)
      triggerToast('Custom Campaign Name added!')
    }
  }

  // Handle Campaign Status Toggle (Pause / Resume)
  const handleStatusToggle = (id) => {
    setCampaigns(prev => prev.map(c => {
      if (c.id === id) {
        const nextStatus = c.status === 'ACTIVE' ? 'PAUSED' : 'ACTIVE'

        // Log activity
        const newAct = {
          id: activities.length + Math.random(),
          campaignId: id,
          date: new Date().toISOString().split('T')[0],
          time: new Date().toTimeString().slice(0, 5),
          type: nextStatus === 'ACTIVE' ? 'Campaign Resumed' : 'Campaign Paused',
          user: 'John Doe',
          text: `Campaign status changed to ${nextStatus}.`
        }
        setActivities(acts => [newAct, ...acts])
        triggerToast(`Campaign status changed to ${nextStatus}!`)
        return { ...c, status: nextStatus }
      }
      return c
    }))
    setActiveActionMenuId(null)
  }

  // Handle Campaign Duplication
  const handleDuplicateCampaign = (c) => {
    const duplicated = {
      ...c,
      id: `CMP-${Math.floor(100 + Math.random() * 900)}`,
      name: `${c.name} (Copy)`,
      status: 'ACTIVE',
      leads: 0,
      qualifiedLeads: 0,
      convertedLeads: 0,
      clicks: 0,
      impressions: 0,
      revenue: 0,
      startDate: new Date().toISOString().split('T')[0]
    }
    setCampaigns([...campaigns, duplicated])
    triggerToast(`Duplicated campaign as "${duplicated.name}"!`)
    setActiveActionMenuId(null)
  }

  // Handle Campaign Archive
  const handleArchiveCampaign = (id) => {
    setCampaigns(campaigns.filter(c => c.id !== id))
    triggerToast('Campaign archived successfully.')
    setActiveActionMenuId(null)
  }

  // Handle Edit Campaign Save
  const handleSaveEdit = (e) => {
    e.preventDefault()
    setCampaigns(prev => prev.map(c => c.id === editingCampaign.id ? editingCampaign : c))

    // Log activity
    const newAct = {
      id: activities.length + Math.random(),
      campaignId: editingCampaign.id,
      date: new Date().toISOString().split('T')[0],
      time: new Date().toTimeString().slice(0, 5),
      type: 'Budget Updated',
      user: 'John Doe',
      text: `Campaign settings and configurations updated.`
    }
    setActivities(acts => [newAct, ...acts])

    setEditingCampaign(null)
    triggerToast('Campaign configuration updated.')
  }

  // Handle Create Campaign Submission
  const handleLaunchCampaign = () => {
    const createdId = `CMP-${Math.floor(100 + Math.random() * 900)}`
    const created = {
      id: createdId,
      name: newCampaign.name,
      type: newCampaign.type || 'Other',
      source: newCampaign.utmSource || 'Direct Ingestion',
      status: 'ACTIVE',
      startDate: newCampaign.startDate || new Date().toISOString().split('T')[0],
      endDate: newCampaign.endDate,
      budget: Number(newCampaign.budget) || 0,
      impressions: 0,
      clicks: 0,
      leads: 0,
      qualifiedLeads: 0,
      convertedLeads: 0,
      revenue: 0,
      owner: newCampaign.owner,
      description: newCampaign.description || 'Custom campaign created.',
      mapping: newCampaign.sources?.join(', ') || 'Custom UTM Mapping',
      assignment: newCampaign.routingRule === 'Round Robin' ? 'Round Robin Distribution' : 'Auto Assignment Rules',
      utmSource: newCampaign.utmSource,
      utmMedium: newCampaign.utmMedium,
      utmName: newCampaign.utmName,
      routingTeam: newCampaign.routingTeam,
      routingRule: newCampaign.routingRule
    }

    setCampaigns([created, ...campaigns])

    // Log Activity
    const launchActivity = {
      id: activities.length + 1,
      campaignId: createdId,
      date: new Date().toISOString().split('T')[0],
      time: new Date().toTimeString().slice(0, 5),
      type: 'Campaign Created',
      user: newCampaign.owner,
      text: `Campaign launched successfully with UTM tracking parameters.`
    }
    setActivities([launchActivity, ...activities])

    // Reset Form
    setNewCampaign({
      name: '',
      description: '',
      type: '',
      owner: 'John Doe (Self)',
      budget: '',
      startDate: new Date().toISOString().split('T')[0],
      endDate: '',
      sources: ['Landing Page'],
      utmSource: '',
      utmMedium: '',
      utmName: '',
      routingTeam: 'Sales Team A',
      routingRule: 'Round Robin',
      notes: '',
      customParams: ''
    })
    setGeneratedUrlVal('https://lms.edu/enroll')
    setActiveTab('CAMPAIGNS')
    triggerToast(`Campaign "${created.name}" launched successfully!`)
  }

  const handleGenerateUrl = () => {
    const baseUrl = 'https://lms.edu/enroll'
    const params = new URLSearchParams()
    if (newCampaign.utmSource) params.append('utm_source', newCampaign.utmSource.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.utmMedium) params.append('utm_medium', newCampaign.utmMedium.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.utmName) params.append('utm_campaign', newCampaign.utmName.toLowerCase().replace(/\s+/g, '_'))
    else if (newCampaign.name) params.append('utm_campaign', newCampaign.name.toLowerCase().replace(/\s+/g, '_'))
    if (newCampaign.type) params.append('utm_content', newCampaign.type.toLowerCase().replace(/\s+/g, '_'))
    const query = params.toString()
    const result = query ? `${baseUrl}?${query}` : baseUrl
    setGeneratedUrlVal(result)
    triggerToast('Tracking URL Generated!')
  }

  const handleCopyUrl = () => {
    navigator.clipboard.writeText(generatedUrlVal)
    triggerToast('Tracking URL copied to clipboard!')
  }

  // Handle add activity log/note
  const handleAddActivity = (e) => {
    e.preventDefault()
    if (!newNote.trim()) return

    const note = {
      id: activities.length + Math.random(),
      campaignId: activeCampaign.id,
      date: new Date().toISOString().split('T')[0],
      time: new Date().toTimeString().slice(0, 5),
      type: 'Budget Updated', // Map type safely
      user: 'John Doe',
      text: newNote
    }

    setActivities([note, ...activities])
    setNewNote('')
    triggerToast('Note logged to activity timeline.')
  }

  // Helper to check indicators for list
  const getCampaignIndicators = (c) => {
    const indicators = []
    const roi = c.budget > 0 ? (((c.revenue || c.convertedLeads * 1500) - c.budget) / c.budget) * 100 : 0
    const cpl = c.leads > 0 ? c.budget / c.leads : 0

    if (roi > 200 && c.convertedLeads > 50) {
      indicators.push({ label: '🔥 Top Performer', style: 'bg-orange-50 border-orange-100 text-orange-700' })
    }
    if (roi > 100) {
      indicators.push({ label: '💰 High ROI', style: 'bg-emerald-50 border-emerald-100 text-emerald-700' })
    }
    if (cpl > 45 && c.leads > 0) {
      indicators.push({ label: '⚠ High CPL', style: 'bg-rose-50 border-rose-100 text-rose-700' })
    }

    // Check if ending soon (within 30 days of 2026-06-04)
    if (c.endDate) {
      const end = new Date(c.endDate)
      const current = new Date('2026-06-04')
      const diffTime = end - current
      const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24))
      if (diffDays > 0 && diffDays <= 30) {
        indicators.push({ label: '⏳ Ending Soon', style: 'bg-amber-50 border-amber-100 text-amber-700' })
      }
    }
    return indicators
  }

  return (
    <div className="w-full relative h-full flex flex-col font-sans select-none bg-[#f8fafc] p-4 sm:p-5 space-y-4 overflow-y-auto">

      {/* Toast Alert */}
      <AnimatePresence>
        {toastMsg && (
          <motion.div
            initial={{ opacity: 0, y: -20, x: '-50%' }}
            animate={{ opacity: 1, y: 0, x: '-50%' }}
            exit={{ opacity: 0, y: -20, x: '-50%' }}
            className="fixed top-6 left-1/2 z-50 bg-slate-955 text-white px-4 py-2.5 rounded-lg shadow-xl border border-slate-800 flex items-center gap-2 text-xs font-bold font-sans"
          >
            <span className="material-symbols-outlined text-[16px] text-green-400">check_circle</span>
            {toastMsg}
          </motion.div>
        )}
      </AnimatePresence>

      {/* TOP HEADER & ACTION BUTTONS */}
      {/* <div className="flex flex-row justify-between items-center  rounded-xl p-2 "> */}
      <div className="text-left">
        <div className="flex items-center gap-2">

          {activeTab === 'DETAILS' && (
            <button
              onClick={() => setActiveTab(prevTab)}
              className="flex items-center gap-1 text-[11px] font-bold text-slate-500 hover:text-indigo-600 transition-colors"
            >
              <span className="material-symbols-outlined text-[14px]">arrow_back</span>
              Back
            </button>
          )}
          {activeTab === 'CREATE_CAMPAIGN' && (
            <button
              onClick={() => { setActiveTab(prevTab); }}
              className="flex items-center gap-1 text-[11px] font-bold text-slate-500 hover:text-indigo-600 transition-colors"
            >
              <span className="material-symbols-outlined text-[14px]">arrow_back</span>
              Back
            </button>
          )}
        </div>

        {/* </div> */}

        {/* Primary CTA on top-right */}

      </div>


      <div className="flex justify-between items-center bg-white border border-slate-200/80 rounded-xl p-1.5 shadow-2xs">
        <div className="flex bg-slate-50 border border-slate-200/60 rounded-lg p-1 gap-1">
          {[
            { id: 'DASHBOARD', label: 'Campaign Dashboard', icon: 'dashboard' },
            { id: 'CAMPAIGNS', label: 'Campaigns', icon: 'campaign' },
            { id: 'REPORTS', label: 'Reports', icon: 'assessment' }
          ].map(tab => (
            <button
              key={tab.id}
              onClick={() => {
                setActiveTab(tab.id)
                setPrevTab(tab.id)
              }}
              className={`px-3 py-1 rounded-md text-[11px] font-bold transition-all flex items-center gap-1.5 cursor-pointer ${activeTab === tab.id
                ? 'bg-white shadow-2xs text-indigo-600 border border-slate-200'
                : 'text-slate-500 hover:text-slate-800'
                }`}
            >
              <span className="material-symbols-outlined text-[14px]">{tab.icon}</span>
              {tab.label}
            </button>
          ))}
        </div>
        {/* TABS NAVIGATION GROUP */}
        {activeTab !== 'CREATE_CAMPAIGN' && (
          <button
            onClick={() => {
              setPrevTab(activeTab)
              setActiveTab('CREATE_CAMPAIGN')
            }}
            className="h-7 px-3 bg-indigo-600 hover:bg-indigo-700 text-white text-[10px] font-bold rounded-[3px] shadow-sm flex items-center gap-1.5 transition-all cursor-pointer whitespace-nowrap"
          >
            <span className="material-symbols-outlined text-[14px]">add</span>
            New Campaign
          </button>
        )}
      </div>

      {/* MAIN VIEW CONDITIONAL RENDERING */}
      <div className="w-full">

        {/* TAB 1: CAMPAIGN DASHBOARD */}
        {activeTab === 'DASHBOARD' && (
          <div className="space-y-4">

            {/* Section 1: KPI Overview */}
            <div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-3 text-left">
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Total Campaigns</span>
                <span className="text-base font-black text-slate-800 block mt-1">{aggregateStats.total} Programs</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Overall marketing list</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Active Campaigns</span>
                <span className="text-base font-black text-indigo-600 block mt-1">{aggregateStats.active} Active</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Currently driving traffic</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Generated Leads</span>
                <span className="text-base font-black text-slate-800 block mt-1">{aggregateStats.leads.toLocaleString()}</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Ingested from forms</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Qualified Leads</span>
                <span className="text-base font-black text-blue-600 block mt-1">{aggregateStats.qualified.toLocaleString()}</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Vetting rate: {((aggregateStats.qualified / (aggregateStats.leads || 1)) * 100).toFixed(0)}%</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Converted Leads</span>
                <span className="text-base font-black text-emerald-600 block mt-1">{aggregateStats.converted.toLocaleString()}</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Enrollments closed</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Overall ROI</span>
                <span className="text-base font-black text-emerald-700 block mt-1">+{aggregateStats.overallRoi.toFixed(0)}%</span>
                <span className="text-[9px] text-slate-450 block mt-0.5">Net Rev: ${(aggregateStats.revenue - aggregateStats.budget).toLocaleString()}</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-extrabold text-slate-400 uppercase tracking-wider block">Average CPL</span>
                <span className="text-base font-black text-indigo-700 block mt-1">${aggregateStats.cpl.toFixed(2)}</span>
                <span className="text-[9px] text-slate-400 block mt-0.5">Budget spend ratio</span>
              </div>
            </div>

            {/* Section 2: Campaign Health Center */}
            <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs text-left">
              <div className="flex items-center justify-between border-b border-slate-100 pb-2.5 mb-3">
                <div className="flex items-center gap-1.5">
                  <span className="material-symbols-outlined text-[16px] text-indigo-650">health_and_safety</span>
                  <h3 className="text-xs font-black text-slate-800">Campaign Health Center</h3>
                </div>
                <span className="text-[9px] font-bold text-slate-400 uppercase">Prioritize Actions</span>
              </div>
              <div className="grid grid-cols-1 md:grid-cols-4 gap-3">
                <div className="bg-amber-50/50 border border-amber-200/70 rounded-lg p-3 flex items-start gap-2.5">
                  <span className="material-symbols-outlined text-[18px] text-amber-600 mt-0.5">hourglass_top</span>
                  <div className="flex-1">
                    <span className="text-[10px] font-black text-amber-800 block">2 Campaigns Ending Soon</span>
                    <p className="text-[9px] text-amber-600 font-medium mt-0.5">MBA Lookalike & Q3 outreach. Extended budgets recommended.</p>
                    <button onClick={() => { setActiveTab('CAMPAIGNS'); setFilterStatus('ACTIVE'); }} className="text-[9px] font-bold text-indigo-700 hover:underline mt-1 block">Review Timelines</button>
                  </div>
                </div>

                <div className="bg-rose-50/50 border border-rose-200/70 rounded-lg p-3 flex items-start gap-2.5">
                  <span className="material-symbols-outlined text-[18px] text-rose-600 mt-0.5">running_with_errors</span>
                  <div className="flex-1">
                    <span className="text-[10px] font-black text-rose-800 block">Campaign Budget 90% Consumed</span>
                    <p className="text-[9px] text-rose-600 font-medium mt-0.5">Google Ads has spent $14,200 of $15,000 allocation.</p>
                    <button onClick={() => { setSelectedCampaignId('CMP-001'); setActiveTab('DETAILS'); setDetailsSubTab('SETTINGS'); }} className="text-[9px] font-bold text-indigo-700 hover:underline mt-1 block">Add Budget</button>
                  </div>
                </div>

                <div className="bg-rose-50/50 border border-rose-200/70 rounded-lg p-3 flex items-start gap-2.5">
                  <span className="material-symbols-outlined text-[18px] text-rose-600 mt-0.5">trending_up</span>
                  <div className="flex-1">
                    <span className="text-[10px] font-black text-rose-800 block">CPL Increased 15%</span>
                    <p className="text-[9px] text-rose-600 font-medium mt-0.5">Meta Ads CPC surge raised Cost Per Lead to $16.90.</p>
                    <button onClick={() => { setSelectedCampaignId('CMP-002'); setActiveTab('DETAILS'); setDetailsSubTab('ANALYTICS'); }} className="text-[9px] font-bold text-indigo-700 hover:underline mt-1 block">Review CPL Trend</button>
                  </div>
                </div>

                <div className="bg-emerald-50/50 border border-emerald-200/70 rounded-lg p-3 flex items-start gap-2.5">
                  <span className="material-symbols-outlined text-[18px] text-emerald-600 mt-0.5">local_fire_department</span>
                  <div className="flex-1">
                    <span className="text-[10px] font-black text-emerald-800 block">Best Performing Campaign</span>
                    <p className="text-[9px] text-emerald-600 font-medium mt-0.5">Scholarship Admissions Drive achieves 11,600% ROI.</p>
                    <button onClick={() => { setSelectedCampaignId('CMP-004'); setActiveTab('DETAILS'); }} className="text-[9px] font-bold text-indigo-700 hover:underline mt-1 block">Scale Campaign</button>
                  </div>
                </div>
              </div>
            </div>

            {/* Section 3 & 4 Grid */}
            <div className="grid grid-cols-1 lg:grid-cols-5 gap-4 text-left">

              {/* Section 3: Master Funnel */}
              <div className="lg:col-span-3 bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs flex flex-col justify-between">
                <div>
                  <h3 className="text-xs font-black text-slate-800 flex items-center gap-1.5 mb-1">
                    <span className="material-symbols-outlined text-[16px] text-indigo-650">filter_alt</span>
                    Master Marketing Conversion Funnel
                  </h3>
                  <p className="text-[10px] text-slate-400">Total pipeline values showing drop-off and conversion velocities.</p>
                </div>

                <div className="space-y-3.5 my-4">
                  {/* Funnel Stage 1 */}
                  <div>
                    <div className="flex justify-between text-[10px] font-bold text-slate-600 mb-1">
                      <span>Total Leads Ingested</span>
                      <span>3,450 Leads (100%)</span>
                    </div>
                    <div className="h-4 bg-slate-100 rounded-md overflow-hidden relative border border-slate-150">
                      <div className="h-full bg-gradient-to-r from-indigo-500 to-indigo-650" style={{ width: '100%' }}></div>
                    </div>
                  </div>

                  {/* Funnel Stage 2 */}
                  <div>
                    <div className="flex justify-between text-[10px] font-bold text-slate-600 mb-1">
                      <span>Marketing Qualified Leads (MQL)</span>
                      <span>2,450 Qualified ({((2450 / 3450) * 100).toFixed(1)}%)</span>
                    </div>
                    <div className="h-4 bg-slate-100 rounded-md overflow-hidden relative border border-slate-150">
                      <div className="h-full bg-gradient-to-r from-indigo-650 to-blue-500" style={{ width: `${(2450 / 3450) * 100}%` }}></div>
                      <span className="absolute right-2 top-0.5 text-[8.5px] font-black text-slate-500">Drop-off: 29.0%</span>
                    </div>
                  </div>

                  {/* Funnel Stage 3 */}
                  <div>
                    <div className="flex justify-between text-[10px] font-bold text-slate-600 mb-1">
                      <span>Converted Enrollments</span>
                      <span>503 Converted ({((503 / 3450) * 100).toFixed(1)}% of total, {((503 / 2450) * 100).toFixed(1)}% of MQL)</span>
                    </div>
                    <div className="h-4 bg-slate-100 rounded-md overflow-hidden relative border border-slate-150">
                      <div className="h-full bg-gradient-to-r from-blue-500 to-emerald-500" style={{ width: `${(503 / 3450) * 100}%` }}></div>
                      <span className="absolute right-2 top-0.5 text-[8.5px] font-black text-slate-500">Drop-off: 79.5%</span>
                    </div>
                  </div>

                  {/* Funnel Stage 4 */}
                  <div>
                    <div className="flex justify-between text-[10px] font-bold text-slate-600 mb-1">
                      <span>Estimated Revenue Generated</span>
                      <span className="text-emerald-700 font-extrabold">$754,500 Generated (ROI: 2005%)</span>
                    </div>
                    <div className="h-4 bg-slate-100 rounded-md overflow-hidden relative border border-slate-150">
                      <div className="h-full bg-gradient-to-r from-emerald-500 to-emerald-600" style={{ width: '100%' }}></div>
                    </div>
                  </div>
                </div>

                <div className="border-t border-slate-100 pt-2 flex justify-between text-[9px] text-slate-400 font-medium">
                  <span>Conversion rate calculated automatically from CRM lead states.</span>
                  <span className="text-indigo-650 font-bold">Standard ROI = (Rev - Cost) / Cost</span>
                </div>
              </div>

              {/* Section 4: Top Campaigns */}
              <div className="lg:col-span-2 bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs flex flex-col justify-between">
                <div>
                  <h3 className="text-xs font-black text-slate-800 flex items-center gap-1.5 mb-1">
                    <span className="material-symbols-outlined text-[16px] text-indigo-650">stars</span>
                    Top Performing Channels
                  </h3>
                  <p className="text-[10px] text-slate-400">High efficiency engines driving the highest enrollment returns.</p>
                </div>

                <div className="space-y-2.5 my-3">
                  {campaigns.slice(0, 3).map(c => {
                    const roi = c.budget > 0 ? (((c.revenue || c.convertedLeads * 1500) - c.budget) / c.budget) * 100 : 0
                    return (
                      <div key={c.id} className="border border-slate-150 rounded-lg p-2.5 hover:bg-slate-50/50 transition-colors flex items-center justify-between">
                        <div className="space-y-0.5">
                          <span className="text-[10px] font-black text-slate-800 block truncate max-w-[150px]">{c.name}</span>
                          <div className="flex gap-1.5 items-center">
                            <span className="text-[9px] font-extrabold text-slate-400">{c.type}</span>
                            <span className="w-1 h-1 rounded-full bg-slate-300"></span>
                            <span className="text-[9px] font-extrabold text-emerald-600">{roi.toFixed(0)}% ROI</span>
                          </div>
                        </div>
                        <div className="flex items-center gap-4 text-right shrink-0">
                          <div>
                            <span className="text-[8px] font-extrabold text-slate-400 uppercase tracking-wider block">Leads / Conv</span>
                            <strong className="text-[10px] text-slate-700 block">{c.leads} / {c.convertedLeads}</strong>
                          </div>
                          <button
                            onClick={() => {
                              setSelectedCampaignId(c.id)
                              setPrevTab('DASHBOARD')
                              setActiveTab('DETAILS')
                            }}
                            className="h-6 px-2 bg-indigo-50 hover:bg-indigo-100 text-indigo-650 border border-indigo-100 text-[9px] font-bold rounded-[3px] transition-all cursor-pointer"
                          >
                            View Details
                          </button>
                        </div>
                      </div>
                    )
                  })}
                </div>

                <div className="border-t border-slate-100 pt-2 text-center">
                  <button
                    onClick={() => setActiveTab('CAMPAIGNS')}
                    className="text-[9px] font-black text-indigo-650 hover:text-indigo-800 transition-colors flex items-center justify-center gap-0.5 mx-auto"
                  >
                    View All Active Campaigns
                    <span className="material-symbols-outlined text-[12px] mt-0.5">arrow_forward</span>
                  </button>
                </div>
              </div>

            </div>

          </div>
        )}

        {/* TAB 2: CAMPAIGNS LIST */}
        {activeTab === 'CAMPAIGNS' && (
          <div className="space-y-4">

            {/* SEARCH AND FILTERS */}
            <div className="flex flex-col sm:flex-row justify-between items-stretch sm:items-center gap-3 bg-white p-3 border border-slate-200/80 rounded-xl shadow-2xs text-left">
              <div className="relative flex-1">
                <span className="material-symbols-outlined text-[16px] text-slate-400 absolute left-3 top-1/2 -translate-y-1/2">search</span>
                <input
                  type="text"
                  value={searchTerm}
                  onChange={(e) => setSearchTerm(e.target.value)}
                  placeholder="Search campaigns by name, owner or ID..."
                  className="w-full h-8 pl-9 pr-3 border border-slate-200 rounded-lg bg-slate-50 text-[11px] font-medium outline-none focus:bg-white focus:border-indigo-500 transition-all placeholder:text-slate-400 text-slate-800"
                />
              </div>

              <div className="flex gap-2">
                <select
                  value={filterType}
                  onChange={(e) => setFilterType(e.target.value)}
                  className="h-8 px-2.5 border border-slate-200 rounded-lg bg-slate-50 text-[10px] font-bold outline-none cursor-pointer hover:bg-slate-100 transition-colors text-slate-700"
                >
                  <option value="ALL">All Channels</option>
                  <option value="Google Ads">Google Ads</option>
                  <option value="Facebook Ads">Meta / Facebook</option>
                  <option value="Email Marketing">Email Marketing</option>
                  <option value="WhatsApp Campaign">WhatsApp</option>
                  <option value="Webinar">Webinars</option>
                </select>

                <select
                  value={filterStatus}
                  onChange={(e) => setFilterStatus(e.target.value)}
                  className="h-8 px-2.5 border border-slate-200 rounded-lg bg-slate-50 text-[10px] font-bold outline-none cursor-pointer hover:bg-slate-100 transition-colors text-slate-700"
                >
                  <option value="ALL">All Statuses</option>
                  <option value="ACTIVE">ACTIVE</option>
                  <option value="PAUSED">PAUSED</option>
                  <option value="COMPLETED">COMPLETED</option>
                </select>
              </div>
            </div>

            {/* PERFORMANCE LIST TABLE */}
            <div className="bg-white border border-slate-200/80 rounded-xl overflow-hidden shadow-2xs text-left relative">
              <table className="w-full min-w-[1000px] border-collapse">
                <thead>
                  <tr className="border-b border-slate-200 bg-slate-50/70 text-[9.5px] font-bold text-slate-455 uppercase tracking-wider select-none text-center">
                    <th className="px-4 py-2.5 text-left w-64">Campaign Details</th>
                    <th className="px-4 py-2.5 w-24">Status</th>
                    <th className="px-4 py-2.5 w-24">Budget</th>
                    <th className="px-4 py-2.5 w-24">Generated Leads</th>
                    <th className="px-4 py-2.5 w-24">Qualified Leads</th>
                    <th className="px-4 py-2.5 w-24">Converted Leads</th>
                    <th className="px-4 py-2.5 w-24">Conv. Rate</th>
                    <th className="px-4 py-2.5 w-24">Est. ROI</th>
                    <th className="px-4 py-2.5 w-32">Owner</th>
                    <th className="px-4 py-2.5 w-16">Actions</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-150 text-[11.5px] font-sans">
                  {filteredCampaigns.length === 0 ? (
                    <tr>
                      <td colSpan={10} className="px-4 py-8 text-center text-slate-400 font-bold">
                        No campaigns matches your search criteria.
                      </td>
                    </tr>
                  ) : (
                    filteredCampaigns.map((c) => {
                      const convRate = c.leads > 0 ? (c.convertedLeads / c.leads) * 100 : 0
                      const roi = c.budget > 0 ? (((c.revenue || c.convertedLeads * 1500) - c.budget) / c.budget) * 1500 / 15 : 0 // Safe mock calculation
                      const indicators = getCampaignIndicators(c)

                      return (
                        <tr key={c.id} className="hover:bg-slate-50/50 transition-colors">
                          <td className="px-4 py-3">
                            <span
                              onClick={() => {
                                setSelectedCampaignId(c.id)
                                setPrevTab('CAMPAIGNS')
                                setActiveTab('DETAILS')
                              }}
                              className="font-bold text-slate-800 hover:text-indigo-650 hover:underline cursor-pointer block text-xs"
                            >
                              {c.name}
                            </span>
                            <div className="flex flex-wrap items-center gap-1.5 mt-1">
                              <span className="text-[9px] text-slate-400 font-semibold">{c.id} • {c.type}</span>
                              {indicators.map((ind, i) => (
                                <span key={i} className={`px-1.5 py-0.2 rounded text-[7.5px] font-extrabold border ${ind.style}`}>
                                  {ind.label}
                                </span>
                              ))}
                            </div>
                          </td>
                          <td className="px-4 py-3 text-center">
                            <span className={`inline-block px-2 py-0.5 rounded text-[8.5px] font-extrabold border ${c.status === 'ACTIVE' ? 'bg-emerald-50 border-emerald-200 text-emerald-700' :
                              c.status === 'PAUSED' ? 'bg-amber-50 border-amber-200 text-amber-700' :
                                'bg-slate-100 border-slate-200 text-slate-500'
                              }`}>{c.status}</span>
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-bold text-slate-800">
                            ${c.budget.toLocaleString()}
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-semibold text-slate-700">
                            +{c.leads.toLocaleString()}
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-semibold text-blue-605">
                            +{c.qualifiedLeads.toLocaleString()}
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-bold text-emerald-600">
                            +{c.convertedLeads.toLocaleString()}
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-semibold text-slate-650">
                            {convRate.toFixed(1)}%
                          </td>
                          <td className="px-4 py-3 text-center font-mono font-black text-emerald-700">
                            +{roi.toFixed(0)}%
                          </td>
                          <td className="px-4 py-3 text-center text-slate-500 font-medium">
                            {c.owner}
                          </td>
                          <td className="px-4 py-3 text-center relative">
                            <button
                              onClick={() => setActiveActionMenuId(activeActionMenuId === c.id ? null : c.id)}
                              className="p-1 hover:bg-slate-100 rounded-lg text-slate-400 hover:text-slate-700 transition-colors cursor-pointer"
                            >
                              <span className="material-symbols-outlined text-[16px] block">more_vert</span>
                            </button>

                            {/* Dropdown Action Menu */}
                            {activeActionMenuId === c.id && (
                              <>
                                <div
                                  className="fixed inset-0 z-10"
                                  onClick={() => setActiveActionMenuId(null)}
                                ></div>
                                <div className="absolute right-4 mt-1 w-36 bg-white border border-slate-200 rounded-lg shadow-lg py-1.5 z-20 text-left font-sans text-xs">
                                  <button
                                    onClick={() => {
                                      setSelectedCampaignId(c.id)
                                      setPrevTab('CAMPAIGNS')
                                      setActiveTab('DETAILS')
                                      setActiveActionMenuId(null)
                                    }}
                                    className="w-full px-3 py-1.5 hover:bg-slate-50 text-slate-700 font-bold flex items-center gap-1.5 cursor-pointer"
                                  >
                                    <span className="material-symbols-outlined text-[14px]">visibility</span>
                                    View Details
                                  </button>
                                  <button
                                    onClick={() => {
                                      setEditingCampaign({ ...c })
                                      setActiveActionMenuId(null)
                                    }}
                                    className="w-full px-3 py-1.5 hover:bg-slate-50 text-slate-700 font-bold flex items-center gap-1.5 cursor-pointer"
                                  >
                                    <span className="material-symbols-outlined text-[14px]">edit</span>
                                    Edit Settings
                                  </button>
                                  <button
                                    onClick={() => handleStatusToggle(c.id)}
                                    className="w-full px-3 py-1.5 hover:bg-slate-50 text-slate-700 font-bold flex items-center gap-1.5 cursor-pointer"
                                  >
                                    <span className="material-symbols-outlined text-[14px]">
                                      {c.status === 'ACTIVE' ? 'pause_circle' : 'play_circle'}
                                    </span>
                                    {c.status === 'ACTIVE' ? 'Pause Campaign' : 'Resume Campaign'}
                                  </button>
                                  <button
                                    onClick={() => handleDuplicateCampaign(c)}
                                    className="w-full px-3 py-1.5 hover:bg-slate-50 text-slate-700 font-bold flex items-center gap-1.5 cursor-pointer"
                                  >
                                    <span className="material-symbols-outlined text-[14px]">content_copy</span>
                                    Duplicate
                                  </button>
                                  <div className="border-t border-slate-100 my-1"></div>
                                  <button
                                    onClick={() => handleArchiveCampaign(c.id)}
                                    className="w-full px-3 py-1.5 hover:bg-slate-50 text-rose-600 font-bold flex items-center gap-1.5 cursor-pointer"
                                  >
                                    <span className="material-symbols-outlined text-[14px]">archive</span>
                                    Archive
                                  </button>
                                </div>
                              </>
                            )}
                          </td>
                        </tr>
                      )
                    })
                  )}
                </tbody>
              </table>
            </div>

          </div>
        )}

        {/* TAB 3: REPORTS */}
        {activeTab === 'REPORTS' && (
          <div className="space-y-4">

            {/* KPI SUMMARY spend/revenue/ROI */}
            <div className="grid grid-cols-2 md:grid-cols-6 gap-3 text-left">
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Total Spend</span>
                <span className="text-base font-black text-slate-800 block mt-1">${aggregateStats.budget.toLocaleString()}</span>
                <span className="text-[8.5px] font-bold text-slate-455 block mt-0.5">Budget allocations</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Gross Revenue</span>
                <span className="text-base font-black text-slate-800 block mt-1">${aggregateStats.revenue.toLocaleString()}</span>
                <span className="text-[8.5px] font-bold text-emerald-600 block mt-0.5">Estimated closed value</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Return on Investment</span>
                <span className="text-base font-black text-emerald-700 block mt-1">+{aggregateStats.overallRoi.toFixed(0)}%</span>
                <span className="text-[8.5px] font-bold text-slate-450 block mt-0.5">Net scale index</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Total Leads</span>
                <span className="text-base font-black text-indigo-600 block mt-1">{aggregateStats.leads.toLocaleString()}</span>
                <span className="text-[8.5px] font-bold text-slate-450 block mt-0.5">Avg Cost/Lead: ${aggregateStats.cpl.toFixed(1)}</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Qualified MQLs</span>
                <span className="text-base font-black text-blue-600 block mt-1">{aggregateStats.qualified.toLocaleString()}</span>
                <span className="text-[8.5px] font-bold text-slate-450 block mt-0.5">Vetted Leads pool</span>
              </div>
              <div className="bg-white border border-slate-200/80 rounded-xl p-3 shadow-2xs">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider block">Enrollments</span>
                <span className="text-base font-black text-emerald-600 block mt-1">{aggregateStats.converted.toLocaleString()}</span>
                <span className="text-[8.5px] font-bold text-slate-450 block mt-0.5">Total closed clients</span>
              </div>
            </div>

            {/* CHARTS TREND PANELS */}
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 text-left">

              {/* Chart 1: Lead & Conversion Trend */}
              <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs space-y-3">
                <div className="flex justify-between items-center">
                  <h3 className="text-xs font-black text-slate-800">Lead & Conversion Volume Trends</h3>
                  <span className="text-[9px] font-bold text-slate-400">Monthly scale</span>
                </div>

                {/* SVG Mock Trend Line Chart */}
                <div className="relative h-44 border border-slate-100 rounded-lg p-2 bg-slate-50/50">
                  <svg className="w-full h-full" viewBox="0 0 400 150" preserveAspectRatio="none">
                    <defs>
                      <linearGradient id="leadGrad" x1="0" y1="0" x2="0" y2="1">
                        <stop offset="0%" stopColor="#4f46e5" stopOpacity="0.25" />
                        <stop offset="100%" stopColor="#4f46e5" stopOpacity="0" />
                      </linearGradient>
                      <linearGradient id="convGrad" x1="0" y1="0" x2="0" y2="1">
                        <stop offset="0%" stopColor="#10b981" stopOpacity="0.25" />
                        <stop offset="100%" stopColor="#10b981" stopOpacity="0" />
                      </linearGradient>
                    </defs>

                    {/* Grid Lines */}
                    <line x1="0" y1="37" x2="400" y2="37" stroke="#e2e8f0" strokeDasharray="3,3" />
                    <line x1="0" y1="75" x2="400" y2="75" stroke="#e2e8f0" strokeDasharray="3,3" />
                    <line x1="0" y1="112" x2="400" y2="112" stroke="#e2e8f0" strokeDasharray="3,3" />

                    {/* Lead Trend Area & Line */}
                    <path d="M 0 120 Q 80 80 160 95 T 320 40 T 400 25 L 400 150 L 0 150 Z" fill="url(#leadGrad)" />
                    <path d="M 0 120 Q 80 80 160 95 T 320 40 T 400 25" fill="none" stroke="#4f46e5" strokeWidth="2.5" />

                    {/* Conversion Trend Area & Line */}
                    <path d="M 0 140 Q 80 120 160 125 T 320 110 T 400 90 L 400 150 L 0 150 Z" fill="url(#convGrad)" />
                    <path d="M 0 140 Q 80 120 160 125 T 320 110 T 400 90" fill="none" stroke="#10b981" strokeWidth="2.5" />
                  </svg>

                  {/* Axis Legend labels */}
                  <div className="absolute bottom-1 left-2 right-2 flex justify-between text-[8px] font-bold text-slate-400 font-mono">
                    <span>Jan 2026</span>
                    <span>Feb 2026</span>
                    <span>Mar 2026</span>
                    <span>Apr 2026</span>
                    <span>May 2026</span>
                    <span>Jun 2026</span>
                  </div>
                </div>
                <div className="flex gap-4 text-[9px] font-bold text-slate-500">
                  <span className="flex items-center gap-1"><span className="w-2.5 h-1 bg-indigo-600 rounded"></span> Leads Ingested</span>
                  <span className="flex items-center gap-1"><span className="w-2.5 h-1 bg-emerald-500 rounded"></span> Converted Enrollments</span>
                </div>
              </div>

              {/* Chart 2: ROI & CPL Trends */}
              <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs space-y-3">
                <div className="flex justify-between items-center">
                  <h3 className="text-xs font-black text-slate-800">CPL Efficiency vs ROI Yield</h3>
                  <span className="text-[9px] font-bold text-slate-400">Weekly cohort</span>
                </div>

                {/* SVG line and bar chart */}
                <div className="relative h-44 border border-slate-100 rounded-lg p-2 bg-slate-50/50">
                  <svg className="w-full h-full" viewBox="0 0 400 150" preserveAspectRatio="none">
                    <defs>
                      <linearGradient id="roiGrad" x1="0" y1="0" x2="0" y2="1">
                        <stop offset="0%" stopColor="#059669" stopOpacity="0.2" />
                        <stop offset="100%" stopColor="#059669" stopOpacity="0" />
                      </linearGradient>
                    </defs>
                    {/* Grid Lines */}
                    <line x1="0" y1="50" x2="400" y2="50" stroke="#e2e8f0" strokeDasharray="3,3" />
                    <line x1="0" y1="100" x2="400" y2="100" stroke="#e2e8f0" strokeDasharray="3,3" />

                    {/* ROI Trend (Line) */}
                    <path d="M 0 110 L 80 90 L 160 50 L 240 60 L 320 30 L 400 20 L 400 150 L 0 150 Z" fill="url(#roiGrad)" />
                    <path d="M 0 110 L 80 90 L 160 50 L 240 60 L 320 30 L 400 20" fill="none" stroke="#059669" strokeWidth="2.5" />

                    {/* CPL Trend (Bars) */}
                    {[
                      { x: 30, h: 40 }, { x: 90, h: 55 }, { x: 150, h: 30 },
                      { x: 210, h: 42 }, { x: 270, h: 25 }, { x: 330, h: 18 }
                    ].map((bar, i) => (
                      <rect key={i} x={bar.x} y={150 - bar.h} width="16" height={bar.h} rx="2" fill="#ef4444" opacity="0.75" />
                    ))}
                  </svg>
                  <div className="absolute bottom-1 left-2 right-2 flex justify-between text-[8px] font-bold text-slate-400 font-mono">
                    <span>Wk 18</span>
                    <span>Wk 19</span>
                    <span>Wk 20</span>
                    <span>Wk 21</span>
                    <span>Wk 22</span>
                    <span>Wk 23</span>
                  </div>
                </div>
                <div className="flex gap-4 text-[9px] font-bold text-slate-500">
                  <span className="flex items-center gap-1"><span className="w-2.5 h-1 bg-emerald-600 rounded"></span> ROI Yield (%)</span>
                  <span className="flex items-center gap-1"><span className="w-2.5 h-2 bg-red-500 rounded-xs"></span> Cost Per Lead (CPL)</span>
                </div>
              </div>

            </div>

            {/* DETAILED TABLES AT BOTTOM */}
            <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs text-left space-y-3.5">
              <div className="flex justify-between items-center">
                <div>
                  <h3 className="text-xs font-black text-slate-800">Campaign Rolled-up Reports</h3>
                  <p className="text-[10px] text-slate-400">Detailed campaign breakdown of clicks, spend efficiency, and channel yields.</p>
                </div>
                <div className="flex items-center gap-2">
                  <ExportButton triggerToast={triggerToast} />
                </div>
              </div>

              <div className="border border-slate-200 rounded-lg overflow-hidden">
                <table className="w-full border-collapse">
                  <thead>
                    <tr className="border-b border-slate-200 bg-slate-50 text-[9.5px] font-bold text-slate-455 uppercase select-none text-center">
                      <th className="px-4 py-2 text-left">Campaign Program</th>
                      <th className="px-4 py-2">Total Budget</th>
                      <th className="px-4 py-2">Impressions</th>
                      <th className="px-4 py-2">Clicks</th>
                      <th className="px-4 py-2">Leads</th>
                      <th className="px-4 py-2">Avg CPL</th>
                      <th className="px-4 py-2">Conversions</th>
                      <th className="px-4 py-2">ROI (%)</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-slate-150 text-[11px] font-sans">
                    {campaigns.map((c, idx) => {
                      const cpl = c.leads > 0 ? c.budget / c.leads : 0
                      const roi = c.budget > 0 ? (((c.revenue || c.convertedLeads * 1500) - c.budget) / c.budget) * 100 : 0
                      return (
                        <tr key={idx} className="hover:bg-slate-50/20">
                          <td className="px-4 py-2.5 font-bold text-slate-750">{c.name}</td>
                          <td className="px-4 py-2.5 text-center font-mono font-semibold">${c.budget.toLocaleString()}</td>
                          <td className="px-4 py-2.5 text-center font-mono text-slate-500">+{c.impressions ? c.impressions.toLocaleString() : 'N/A'}</td>
                          <td className="px-4 py-2.5 text-center font-mono text-slate-550">+{c.clicks ? c.clicks.toLocaleString() : 'N/A'}</td>
                          <td className="px-4 py-2.5 text-center font-mono font-bold text-slate-800">+{c.leads}</td>
                          <td className="px-4 py-2.5 text-center font-mono text-slate-650">${cpl.toFixed(1)}</td>
                          <td className="px-4 py-2.5 text-center font-mono font-bold text-emerald-600">+{c.convertedLeads}</td>
                          <td className={`px-4 py-2.5 text-center font-mono font-extrabold ${roi > 0 ? 'text-emerald-700' : 'text-slate-500'}`}>{roi.toFixed(0)}%</td>
                        </tr>
                      )
                    })}
                  </tbody>
                </table>
              </div>
            </div>

          </div>
        )}

        {/* DETAILS SCREEN (Drill-down from clicks on campaigns) */}
        {activeTab === 'DETAILS' && (
          <div className="grid grid-cols-1 lg:grid-cols-4 gap-4 text-left">

            {/* Left Sidebar: Selector */}
            <div className="lg:col-span-1 bg-white border border-slate-200/80 rounded-xl p-3.5 shadow-2xs space-y-3.5 flex flex-col">
              <div className="border-b border-slate-100 pb-2">
                <span className="text-[9px] font-black text-slate-400 uppercase tracking-wider">Campaign Explorer</span>
              </div>
              <div className="space-y-1 max-h-[450px] overflow-y-auto pr-1">
                {campaigns.map(c => (
                  <button
                    key={c.id}
                    onClick={() => setSelectedCampaignId(c.id)}
                    className={`w-full px-3 py-2 text-[11px] font-bold rounded-lg text-left transition-all border ${selectedCampaignId === c.id
                      ? 'bg-indigo-50 border-indigo-200 text-indigo-700'
                      : 'text-slate-600 hover:bg-slate-50 border-transparent'
                      }`}
                  >
                    <span className="block truncate font-bold text-[11.5px]">{c.name}</span>
                    <div className="flex justify-between items-center mt-1">
                      <span className="text-[8px] text-slate-400 font-extrabold uppercase">{c.type}</span>
                      <span className="text-[8px] font-bold font-mono text-slate-500">${c.budget.toLocaleString()}</span>
                    </div>
                  </button>
                ))}
              </div>
            </div>

            {/* Right Column: Detailed Performance Cockpit */}
            <div className="lg:col-span-3 space-y-4">

              {/* Campaign Profile Header */}
              <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs">
                <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3">
                  <div>
                    <div className="flex items-center gap-2">
                      <span className="text-[8.5px] font-extrabold text-indigo-700 bg-indigo-50 border border-indigo-100 px-2 py-0.5 rounded uppercase">
                        {activeCampaign.type}
                      </span>
                      <span className={`inline-block px-1.5 py-0.2 rounded text-[7.5px] font-black border ${activeCampaign.status === 'ACTIVE' ? 'bg-emerald-50 border-emerald-100 text-emerald-700' : 'bg-slate-50 border-slate-100 text-slate-500'
                        }`}>
                        {activeCampaign.status}
                      </span>
                    </div>
                    <h2 className="text-base font-black text-slate-800 mt-1">{activeCampaign.name}</h2>
                    <p className="text-[10px] text-slate-400 mt-0.5">Owner: {activeCampaign.owner} • Timeline: {activeCampaign.startDate} to {activeCampaign.endDate || 'Ongoing'}</p>
                  </div>

                  {/* Sub tab navigation switcher */}
                  <div className="flex bg-slate-100 border border-slate-200/60 rounded-lg p-0.5">
                    {[
                      { id: 'OVERVIEW', label: 'Overview' },
                      { id: 'LEADS', label: 'Leads' },
                      { id: 'ACTIVITIES', label: 'Activities' },
                      { id: 'ANALYTICS', label: 'Analytics' },
                      { id: 'SETTINGS', label: 'Settings' }
                    ].map(sub => (
                      <button
                        key={sub.id}
                        onClick={() => setDetailsSubTab(sub.id)}
                        className={`px-2.5 py-1 text-[10px] font-black rounded-md transition-all cursor-pointer ${detailsSubTab === sub.id ? 'bg-white shadow-2xs text-slate-800' : 'text-slate-500 hover:text-slate-800'
                          }`}
                      >
                        {sub.label}
                      </button>
                    ))}
                  </div>
                </div>

                {/* KPI metrics row */}
                <div className="grid grid-cols-3 md:grid-cols-6 gap-3 mt-4 pt-4 border-t border-slate-100 text-left">
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">Leads Generated</span>
                    <strong className="text-sm font-black text-slate-800 block mt-0.5">+{activeCampaign.leads}</strong>
                  </div>
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">Qualified Leads</span>
                    <strong className="text-sm font-black text-blue-600 block mt-0.5">+{activeCampaign.qualifiedLeads}</strong>
                  </div>
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">Converted</span>
                    <strong className="text-sm font-black text-emerald-600 block mt-0.5">+{activeCampaign.convertedLeads}</strong>
                  </div>
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">Spend Budget</span>
                    <strong className="text-sm font-black text-slate-800 block mt-0.5">${activeCampaign.budget.toLocaleString()}</strong>
                  </div>
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">Estimated ROI</span>
                    <strong className="text-sm font-black text-emerald-700 block mt-0.5">
                      +{activeCampaign.budget > 0 ? ((((activeCampaign.revenue || activeCampaign.convertedLeads * 1500) - activeCampaign.budget) / activeCampaign.budget) * 100).toFixed(0) : 0}%
                    </strong>
                  </div>
                  <div>
                    <span className="text-[8px] font-black text-slate-400 uppercase">CPL Avg</span>
                    <strong className="text-sm font-black text-indigo-700 block mt-0.5">
                      ${activeCampaign.leads > 0 ? (activeCampaign.budget / activeCampaign.leads).toFixed(2) : '0.00'}
                    </strong>
                  </div>
                </div>
              </div>

              {/* Step Funnel visualization */}
              <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs text-left">
                <h3 className="text-xs font-black text-slate-800 mb-3 flex items-center gap-1">
                  <span className="material-symbols-outlined text-[15px] text-indigo-655">stacked_bar_chart</span>
                  Campaign Conversion Pipeline
                </h3>
                <div className="flex flex-col md:flex-row items-stretch gap-2.5">
                  {[
                    { label: 'Impressions', val: activeCampaign.impressions || 12000, pct: '100%', sub: 'Reach scale' },
                    { label: 'Clicks', val: activeCampaign.clicks || 1200, pct: `${(((activeCampaign.clicks || 1200) / (activeCampaign.impressions || 12000)) * 100).toFixed(1)}%`, sub: 'CTR rate' },
                    { label: 'Leads', val: activeCampaign.leads, pct: `${((activeCampaign.leads / (activeCampaign.clicks || 1200)) * 100).toFixed(1)}%`, sub: 'Click-to-Lead' },
                    { label: 'Qualified', val: activeCampaign.qualifiedLeads, pct: `${((activeCampaign.qualifiedLeads / (activeCampaign.leads || 1)) * 100).toFixed(1)}%`, sub: 'Vetting' },
                    { label: 'Converted', val: activeCampaign.convertedLeads, pct: `${((activeCampaign.convertedLeads / (activeCampaign.qualifiedLeads || 1)) * 100).toFixed(1)}%`, sub: 'Conversion' }
                  ].map((stage, idx) => (
                    <div key={idx} className="flex-1 bg-slate-50 border border-slate-150 rounded-lg p-2.5 flex flex-col justify-between relative overflow-hidden">
                      <div className="absolute right-0 top-0 translate-x-1 translate-y-1 text-slate-200 opacity-20 select-none">
                        <span className="text-5xl font-black">{idx + 1}</span>
                      </div>
                      <div className="space-y-0.5 z-10">
                        <span className="text-[8px] font-black text-slate-400 uppercase block">{stage.label}</span>
                        <strong className="text-base font-black text-slate-800 block">{stage.val.toLocaleString()}</strong>
                      </div>
                      <div className="mt-3.5 z-10">
                        <span className="text-[9px] font-bold text-indigo-700 bg-indigo-50 border border-indigo-100/60 px-1.5 py-0.5 rounded block text-center">
                          {stage.pct} <span className="text-[8px] font-medium text-slate-400 block sm:inline">{stage.sub}</span>
                        </span>
                      </div>
                    </div>
                  ))}
                </div>
              </div>

              {/* Sub tabs Rendering */}
              <div className="w-full">

                {/* SUB TAB: OVERVIEW */}
                {detailsSubTab === 'OVERVIEW' && (
                  <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div className="space-y-3">
                      <div>
                        <span className="text-[8.5px] font-black text-slate-400 uppercase tracking-wider block">Description</span>
                        <p className="text-xs text-slate-600 mt-1 leading-relaxed">{activeCampaign.description || 'No description provided.'}</p>
                      </div>
                      <div>
                        <span className="text-[8.5px] font-black text-slate-400 uppercase tracking-wider block">Tracking URL Parameters</span>
                        <div className="bg-slate-50 border border-slate-150 rounded-md p-2 mt-1 text-[10px] font-mono text-slate-600 select-all cursor-text leading-tight break-all">
                          https://lms.edu/enroll?utm_source={activeCampaign.utmSource || 'google'}&utm_medium={activeCampaign.utmMedium || 'cpc'}&utm_campaign={activeCampaign.utmName || 'camp'}
                        </div>
                      </div>
                    </div>

                    <div className="grid grid-cols-2 gap-3.5">
                      <div className="bg-slate-50/50 border border-slate-150 rounded-lg p-3">
                        <span className="text-[8px] font-black text-slate-400 uppercase block">Lead Source Setup</span>
                        <strong className="text-xs text-slate-700 mt-1 block truncate">{activeCampaign.source}</strong>
                      </div>
                      <div className="bg-slate-50/50 border border-slate-150 rounded-lg p-3">
                        <span className="text-[8px] font-black text-slate-400 uppercase block">Source Mapping Integration</span>
                        <strong className="text-xs text-slate-700 mt-1 block truncate">{activeCampaign.mapping}</strong>
                      </div>
                      <div className="bg-slate-50/50 border border-slate-150 rounded-lg p-3">
                        <span className="text-[8px] font-black text-slate-400 uppercase block">Assignment Routing</span>
                        <strong className="text-xs text-slate-700 mt-1 block truncate">{activeCampaign.assignment}</strong>
                      </div>
                      <div className="bg-slate-50/50 border border-slate-150 rounded-lg p-3">
                        <span className="text-[8px] font-black text-slate-400 uppercase block">Target Routing Team</span>
                        <strong className="text-xs text-slate-700 mt-1 block truncate">{activeCampaign.routingTeam || 'Sales Team A'}</strong>
                      </div>
                    </div>
                  </div>
                )}

                {/* SUB TAB: LEADS */}
                {detailsSubTab === 'LEADS' && (
                  <div className="bg-white border border-slate-200/80 rounded-xl overflow-hidden shadow-2xs">
                    <table className="w-full border-collapse">
                      <thead>
                        <tr className="border-b border-slate-200 bg-slate-50 text-[9px] font-bold text-slate-500 uppercase tracking-wider select-none text-center">
                          <th className="px-4 py-2 text-left">Lead Name</th>
                          <th className="px-4 py-2">Email Address</th>
                          <th className="px-4 py-2">Score</th>
                          <th className="px-4 py-2">Pipeline State</th>
                          <th className="px-4 py-2">Assigned Agent</th>
                          <th className="px-4 py-2">Last Activity</th>
                          <th className="px-4 py-2 w-20">Actions</th>
                        </tr>
                      </thead>
                      <tbody className="divide-y divide-slate-150 text-[11px] font-sans">
                        {initialLeadsList
                          .filter(l => l.campaignId === activeCampaign.id || activeCampaign.id === 'CMP-001')
                          .map((lead, idx) => (
                            <tr key={idx} className="hover:bg-slate-50/30">
                              <td className="px-4 py-2 font-bold text-slate-705">{lead.name}</td>
                              <td className="px-4 py-2 text-center text-slate-550 font-mono">
                                {isMasked ? maskEmail(lead.email) : lead.email}
                              </td>
                              <td className="px-4 py-2 text-center font-mono">
                                <span className={`inline-block px-1.5 py-0.2 rounded font-bold text-[9px] ${lead.score >= 80 ? 'bg-emerald-50 text-emerald-700 border border-emerald-100' :
                                  lead.score >= 50 ? 'bg-indigo-50 text-indigo-700 border border-indigo-100' :
                                    'bg-slate-50 text-slate-500 border border-slate-200'
                                  }`}>
                                  {lead.score} pts
                                </span>
                              </td>
                              <td className="px-4 py-2 text-center">
                                <span className={`inline-block px-1.5 py-0.2 rounded text-[8px] font-extrabold ${lead.status === 'Converted' ? 'bg-emerald-50 text-emerald-700 border border-emerald-100' :
                                  lead.status === 'Qualified' ? 'bg-indigo-50 text-indigo-700 border border-indigo-100' :
                                    'bg-slate-100 text-slate-650 border border-slate-200'
                                  }`}>{lead.status}</span>
                              </td>
                              <td className="px-4 py-2 text-center text-slate-550">{lead.assignedTo}</td>
                              <td className="px-4 py-2 text-center text-slate-400 font-medium text-[10px]">{lead.lastActivity}</td>
                              <td className="px-4 py-2 text-center">
                                <button
                                  onClick={() => triggerToast(`Routing to detailed profile for ${lead.name}...`)}
                                  className="h-5 px-2 border border-slate-200 bg-white hover:bg-slate-50 text-[9px] font-black text-slate-600 rounded-[3px] transition-colors cursor-pointer"
                                >
                                  View Lead
                                </button>
                              </td>
                            </tr>
                          ))}
                      </tbody>
                    </table>
                  </div>
                )}

                {/* SUB TAB: ACTIVITIES */}
                {detailsSubTab === 'ACTIVITIES' && (
                  <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs space-y-4">
                    <form onSubmit={handleAddActivity} className="flex gap-2">
                      <input
                        type="text"
                        value={newNote}
                        onChange={(e) => setNewNote(e.target.value)}
                        placeholder="Log custom campaigns updates, note, budget or media task logs..."
                        className="flex-1 h-8 px-3 border border-slate-200 rounded-lg bg-slate-50 text-[11px] outline-none focus:bg-white focus:border-indigo-500 text-slate-800"
                      />
                      <button
                        type="submit"
                        className="h-8 px-3 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg text-xs font-bold transition-all cursor-pointer"
                      >
                        Add note
                      </button>
                    </form>

                    {/* Check list style timeline activities */}
                    <div className="space-y-3 pt-2">
                      {activities
                        .filter(a => a.campaignId === activeCampaign.id)
                        .map((act) => (
                          <div key={act.id} className="border-l-2 border-indigo-500 pl-3 py-0.5 flex items-start gap-2">
                            <div className="flex-1 text-left">
                              <div className="flex justify-between items-center text-[9px]">
                                <strong className="text-slate-600 font-black uppercase tracking-wider">{act.type} • Logged by {act.user}</strong>
                                <span className="text-slate-400 font-mono">{act.date} {act.time}</span>
                              </div>
                              <p className="text-[11px] text-slate-650 mt-0.5 leading-relaxed">{act.text}</p>
                            </div>
                          </div>
                        ))}
                    </div>
                  </div>
                )}

                {/* SUB TAB: ANALYTICS */}
                {detailsSubTab === 'ANALYTICS' && (
                  <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs space-y-4">
                    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">

                      {/* Trend lines */}
                      <div className="border border-slate-150 rounded-lg p-3">
                        <span className="text-[9px] font-black text-slate-400 uppercase tracking-widest block">Lead generation velocity</span>
                        <div className="h-32 mt-2">
                          <svg className="w-full h-full" viewBox="0 0 300 100" preserveAspectRatio="none">
                            <defs>
                              <linearGradient id="anaGrad" x1="0" y1="0" x2="0" y2="1">
                                <stop offset="0%" stopColor="#4f46e5" stopOpacity="0.2" />
                                <stop offset="100%" stopColor="#4f46e5" stopOpacity="0" />
                              </linearGradient>
                            </defs>
                            <path d="M0 80 Q 75 50 150 40 T 300 15 L 300 100 L 0 100 Z" fill="url(#anaGrad)" />
                            <path d="M0 80 Q 75 50 150 40 T 300 15" fill="none" stroke="#4f46e5" strokeWidth="2" />
                          </svg>
                        </div>
                        <div className="flex justify-between text-[8px] font-mono text-slate-400 mt-1">
                          <span>Wk 1</span>
                          <span>Wk 2</span>
                          <span>Wk 3</span>
                          <span>Wk 4</span>
                        </div>
                      </div>

                      <div className="border border-slate-150 rounded-lg p-3">
                        <span className="text-[9px] font-black text-slate-400 uppercase tracking-widest block">Conversion Rates Efficiency</span>
                        <div className="h-32 mt-2">
                          <svg className="w-full h-full" viewBox="0 0 300 100" preserveAspectRatio="none">
                            <defs>
                              <linearGradient id="anaGrad2" x1="0" y1="0" x2="0" y2="1">
                                <stop offset="0%" stopColor="#10b981" stopOpacity="0.2" />
                                <stop offset="100%" stopColor="#10b981" stopOpacity="0" />
                              </linearGradient>
                            </defs>
                            <path d="M0 90 L 75 60 L 150 70 L 225 45 L 300 35 L 300 100 L 0 100 Z" fill="url(#anaGrad2)" />
                            <path d="M0 90 L 75 60 L 150 70 L 225 45 L 300 35" fill="none" stroke="#10b981" strokeWidth="2" />
                          </svg>
                        </div>
                        <div className="flex justify-between text-[8px] font-mono text-slate-400 mt-1">
                          <span>Wk 1</span>
                          <span>Wk 2</span>
                          <span>Wk 3</span>
                          <span>Wk 4</span>
                        </div>
                      </div>

                    </div>
                  </div>
                )}

                {/* SUB TAB: SETTINGS (Editable Routing/General rules) */}
                {detailsSubTab === 'SETTINGS' && (
                  <div className="bg-white border border-slate-200/80 rounded-xl p-4 shadow-2xs space-y-4">
                    <form onSubmit={(e) => {
                      e.preventDefault();
                      triggerToast('Routing rules and mappings updated successfully!');
                    }} className="space-y-3">
                      <div className="grid grid-cols-1 md:grid-cols-2 gap-3.5">
                        <div>
                          <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">
                            Team Lead Assignment
                          </label>
                          <select
                            defaultValue={activeCampaign.routingTeam || 'Sales Team A'}
                            className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                          >
                            <option value="Sales Team A">Sales Team A</option>
                            <option value="Sales Team B">Sales Team B</option>
                            <option value="Counselors Pool">Counselors Pool</option>
                          </select>
                        </div>
                        <div>
                          <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">
                            Lead Routing Mode
                          </label>
                          <select
                            defaultValue={activeCampaign.routingRule || 'Round Robin'}
                            className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                          >
                            <option value="Round Robin">Round Robin Allocation</option>
                            <option value="Manual Assignment">Manual Admin Assignment</option>
                            <option value="Auto Assignment Rules">Auto Rule-Based Assignment</option>
                          </select>
                        </div>
                      </div>

                      <div>
                        <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">
                          Source Integration Hook
                        </label>
                        <input
                          type="text"
                          defaultValue={activeCampaign.mapping}
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                        />
                      </div>

                      <div className="flex justify-end pt-2 border-t border-slate-100">
                        <button
                          type="submit"
                          className="h-7 px-4 bg-indigo-650 hover:bg-indigo-700 text-white text-[10px] font-bold rounded-[3px] transition-colors cursor-pointer"
                        >
                          Save Routing Configurations
                        </button>
                      </div>
                    </form>
                  </div>
                )}

              </div>

            </div>
          </div>
        )}

        {/* TAB 4: CREATE CAMPAIGN SETUP FORM */}
        {activeTab === 'CREATE_CAMPAIGN' && (
          <div className="bg-white border border-slate-200/80 rounded-xl p-6 shadow-xs max-w-2xl mx-auto text-left space-y-6">

            <div className="border-b border-slate-100 pb-3">
              <h2 className="text-sm font-black text-slate-800 uppercase tracking-wider">Campaign Ingest & Inbound Tracking Setup</h2>
              <p className="text-[11px] text-slate-400 mt-1">Configure lead attribution parameters and UTM query string parameters.</p>
            </div>

            <form onSubmit={(e) => { e.preventDefault(); handleLaunchCampaign(); }} className="space-y-6">

              {/* SECTION 1: CAMPAIGN INFORMATION */}
              <div className="space-y-3">
                <div className="flex items-center justify-between border-b border-slate-100 pb-1.5">
                  <h3 className="text-xs font-bold text-slate-700 uppercase tracking-wide">1. Campaign Information</h3>
                  <span className="text-[9px] text-slate-400 font-bold">* Required fields</span>
                </div>
                <p className="text-[10px] text-slate-400 font-medium">
                  Define the campaign that will be used for lead attribution and tracking.
                </p>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label className="text-[10px] font-bold text-slate-600 block mb-1">Campaign Type *</label>
                    <select
                      value={newCampaign.type}
                      onChange={(e) => setNewCampaign({ ...newCampaign, type: e.target.value })}
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-semibold outline-none focus:bg-white focus:border-indigo-500 text-slate-800"
                      required
                    >
                      <option value="">Select Channel Type</option>
                      <option value="Google Ads">Google Ads</option>
                      <option value="Facebook Ads">Facebook / Meta Ads</option>
                      <option value="Email Marketing">Email Marketing</option>
                      <option value="WhatsApp Campaign">WhatsApp Campaign</option>
                      <option value="SMS Campaign">SMS Campaign</option>
                      <option value="Webinar">Webinar Event</option>
                    </select>
                  </div>
                  <div>
                    <label className="text-[10px] font-bold text-slate-600 block mb-1">Campaign Name *</label>
                    <input
                      type="text"
                      value={newCampaign.name}
                      onChange={(e) => setNewCampaign({ ...newCampaign, name: e.target.value })}
                      placeholder="e.g. Google Ads MBA Admissions 2026"
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white focus:border-indigo-500 text-slate-800 placeholder:text-slate-400"
                      required
                    />
                  </div>
                </div>
              </div>

              {/* SECTION 2: TRACKING CONFIGURATION */}
              <div className="space-y-3">
                <div className="border-b border-slate-100 pb-1.5">
                  <h3 className="text-xs font-bold text-slate-700 uppercase tracking-wide">2. Tracking Configuration</h3>
                </div>
                <p className="text-[10px] text-slate-400 font-medium">
                  Group all UTM fields together for parameter tracking.
                </p>

                <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label className="text-[10px] font-bold text-slate-600 block mb-1">UTM Source *</label>
                    <input
                      type="text"
                      value={newCampaign.utmSource}
                      onChange={(e) => setNewCampaign({ ...newCampaign, utmSource: e.target.value })}
                      placeholder="e.g. google"
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white focus:border-indigo-500 text-slate-800 placeholder:text-slate-400"
                      required
                    />
                    <span className="text-[9px] text-slate-400 block mt-1">Source: google</span>
                  </div>
                  <div>
                    <label className="text-[10px] font-bold text-slate-600 block mb-1">UTM Medium *</label>
                    <input
                      type="text"
                      value={newCampaign.utmMedium}
                      onChange={(e) => setNewCampaign({ ...newCampaign, utmMedium: e.target.value })}
                      placeholder="e.g. cpc"
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white focus:border-indigo-500 text-slate-800 placeholder:text-slate-400"
                      required
                    />
                    <span className="text-[9px] text-slate-400 block mt-1">Medium: cpc</span>
                  </div>
                  <div>
                    <label className="text-[10px] font-bold text-slate-600 block mb-1">UTM Campaign Name *</label>
                    <input
                      type="text"
                      value={newCampaign.utmName}
                      onChange={(e) => setNewCampaign({ ...newCampaign, utmName: e.target.value })}
                      placeholder="e.g. mba_admissions_2026"
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white focus:border-indigo-500 text-slate-800 placeholder:text-slate-400"
                      required
                    />
                    <span className="text-[9px] text-slate-400 block mt-1">Campaign: mba_admissions_2026</span>
                  </div>
                </div>
              </div>

              {/* SECTION 3: GENERATED TRACKING URL */}
              <div className="space-y-3">
                <div className="border-b border-slate-100 pb-1.5">
                  <h3 className="text-xs font-bold text-slate-700 uppercase tracking-wide">3. Generated URL</h3>
                </div>

                <div className="bg-slate-50 border border-slate-200 rounded-xl p-4 space-y-3">
                  <div className="flex justify-between items-center">
                    <span className="text-[10px] font-bold text-slate-600 uppercase tracking-wide">Tracking URL Preview</span>
                    <div className="flex gap-2">
                      <button
                        type="button"
                        onClick={handleGenerateUrl}
                        className="h-6 px-2.5 border border-slate-200 bg-white hover:bg-slate-50 text-slate-700 text-[10px] font-bold rounded-[3px] shadow-sm transition-all cursor-pointer"
                      >
                        Generate URL
                      </button>
                      <button
                        type="button"
                        onClick={handleCopyUrl}
                        className="h-6 px-2.5 border border-slate-200 bg-white hover:bg-slate-50 text-slate-700 text-[10px] font-bold rounded-[3px] shadow-sm transition-all cursor-pointer"
                      >
                        Copy URL
                      </button>
                    </div>
                  </div>
                  <input
                    type="text"
                    readOnly
                    value={generatedUrlVal}
                    className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-100 text-[10px] font-mono outline-none text-slate-600 select-all cursor-text"
                  />
                </div>
              </div>

              {/* OPTIONAL ADVANCED SECTION */}
              <div className="space-y-2">
                <button
                  type="button"
                  onClick={() => setShowAdvancedSettings(!showAdvancedSettings)}
                  className="w-full flex items-center justify-between py-2 border-t border-b border-slate-100 text-[11px] font-black text-slate-700 hover:text-indigo-650 transition-colors cursor-pointer"
                >
                  <span>Advanced Settings</span>
                  <span className="material-symbols-outlined text-[16px]">
                    {showAdvancedSettings ? 'keyboard_arrow_up' : 'keyboard_arrow_down'}
                  </span>
                </button>

                {showAdvancedSettings && (
                  <div className="p-4 bg-slate-50/50 border border-slate-150 rounded-xl space-y-4 text-left">
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div>
                        <label className="text-[10px] font-bold text-slate-600 block mb-1">Campaign Owner</label>
                        <input
                          type="text"
                          value={newCampaign.owner}
                          onChange={(e) => setNewCampaign({ ...newCampaign, owner: e.target.value })}
                          placeholder="John Doe"
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-800"
                        />
                      </div>
                      <div>
                        <label className="text-[10px] font-bold text-slate-600 block mb-1">Campaign Budget ($)</label>
                        <input
                          type="number"
                          value={newCampaign.budget}
                          onChange={(e) => setNewCampaign({ ...newCampaign, budget: e.target.value })}
                          placeholder="5000"
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-800"
                        />
                      </div>
                      <div>
                        <label className="text-[10px] font-bold text-slate-600 block mb-1">Lead Routing Mode</label>
                        <select
                          value={newCampaign.routingRule}
                          onChange={(e) => setNewCampaign({ ...newCampaign, routingRule: e.target.value })}
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-semibold outline-none focus:border-indigo-500 text-slate-800"
                        >
                          <option value="Round Robin">Round Robin Distribution</option>
                          <option value="Manual Assignment">Manual Assignment Rules</option>
                          <option value="Auto Assignment Rules">Auto Rule Engine</option>
                        </select>
                      </div>
                    </div>

                    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                      <div>
                        <label className="text-[10px] font-bold text-slate-600 block mb-1">Start Date</label>
                        <input
                          type="date"
                          value={newCampaign.startDate}
                          onChange={(e) => setNewCampaign({ ...newCampaign, startDate: e.target.value })}
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-850"
                        />
                      </div>
                      <div>
                        <label className="text-[10px] font-bold text-slate-600 block mb-1">End Date</label>
                        <input
                          type="date"
                          value={newCampaign.endDate}
                          onChange={(e) => setNewCampaign({ ...newCampaign, endDate: e.target.value })}
                          className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-850"
                        />
                      </div>
                    </div>

                    <div>
                      <label className="text-[10px] font-bold text-slate-600 block mb-1">Description / Target Goal</label>
                      <textarea
                        value={newCampaign.description}
                        onChange={(e) => setNewCampaign({ ...newCampaign, description: e.target.value })}
                        rows={2}
                        placeholder="Target goals, ad groups or campaign brief..."
                        className="w-full p-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-800"
                      />
                    </div>

                    <div>
                      <label className="text-[10px] font-bold text-slate-600 block mb-1">Notes</label>
                      <textarea
                        value={newCampaign.notes || ''}
                        onChange={(e) => setNewCampaign({ ...newCampaign, notes: e.target.value })}
                        rows={2}
                        placeholder="Internal comments, team notes..."
                        className="w-full p-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-800"
                      />
                    </div>

                    <div>
                      <label className="text-[10px] font-bold text-slate-600 block mb-1">Custom Tracking Parameters</label>
                      <input
                        type="text"
                        value={newCampaign.customParams || ''}
                        onChange={(e) => setNewCampaign({ ...newCampaign, customParams: e.target.value })}
                        placeholder="e.g. utm_content=banner_v1&utm_term=engineering"
                        className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-white text-[11px] font-medium outline-none focus:border-indigo-500 text-slate-800 placeholder:text-slate-400"
                      />
                    </div>
                  </div>
                )}
              </div>

              {/* PRIMARY ACTIONS FOOTER */}
              <div className="flex justify-between items-center pt-3 border-t border-slate-100">
                <button
                  type="button"
                  onClick={() => { setActiveTab(prevTab); }}
                  className="h-7 px-3 border border-slate-200 bg-white hover:bg-slate-50 text-slate-700 text-[10px] font-bold rounded-[3px] shadow-sm transition-all cursor-pointer"
                >
                  Cancel
                </button>

                <div className="flex gap-2">
                  <button
                    type="button"
                    onClick={handleGenerateUrl}
                    className="h-7 px-3 border border-slate-200 bg-white hover:bg-slate-50 text-slate-700 text-[10px] font-bold rounded-[3px] shadow-sm transition-all cursor-pointer"
                  >
                    Generate URL
                  </button>
                  <button
                    type="submit"
                    disabled={!(newCampaign.type && newCampaign.name && newCampaign.utmSource && newCampaign.utmMedium && newCampaign.utmName)}
                    className="h-7 px-4 bg-indigo-650 hover:bg-indigo-700 text-white text-[10px] font-bold rounded-[3px] shadow-sm transition-all cursor-pointer disabled:bg-slate-200 disabled:text-slate-400 disabled:cursor-not-allowed"
                  >
                    Create Campaign
                  </button>
                </div>
              </div>

            </form>
          </div>
        )}
      </div>

      {/* EDIT CAMPAIGN MODAL (Inline Modal) */}
      <AnimatePresence>
        {editingCampaign && (
          <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 backdrop-blur-xs">
            <motion.div
              initial={{ opacity: 0, scale: 0.95 }}
              animate={{ opacity: 1, scale: 1 }}
              exit={{ opacity: 0, scale: 0.95 }}
              className="bg-white border border-slate-250 rounded-xl p-5 shadow-2xl max-w-md w-full mx-4 text-left space-y-4"
            >
              <div>
                <h3 className="text-xs font-black text-slate-800 uppercase tracking-widest">
                  Configure Campaign Properties
                </h3>
                <p className="text-[10px] text-slate-400 mt-0.5">
                  Update budget allocations, statuses, dates, and core attributes.
                </p>
              </div>

              <form onSubmit={handleSaveEdit} className="space-y-3">
                <div>
                  <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">Campaign Name</label>
                  <input
                    type="text"
                    value={editingCampaign.name}
                    onChange={(e) => setEditingCampaign({ ...editingCampaign, name: e.target.value })}
                    className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                  />
                </div>

                <div className="grid grid-cols-2 gap-3">
                  <div>
                    <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">Status</label>
                    <select
                      value={editingCampaign.status}
                      onChange={(e) => setEditingCampaign({ ...editingCampaign, status: e.target.value })}
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-semibold outline-none focus:bg-white text-slate-800"
                    >
                      <option value="ACTIVE">ACTIVE</option>
                      <option value="PAUSED">PAUSED</option>
                      <option value="COMPLETED">COMPLETED</option>
                    </select>
                  </div>
                  <div>
                    <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">Budget Allocation ($)</label>
                    <input
                      type="number"
                      value={editingCampaign.budget}
                      onChange={(e) => setEditingCampaign({ ...editingCampaign, budget: Number(e.target.value) })}
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                    />
                  </div>
                </div>

                <div className="grid grid-cols-2 gap-3">
                  <div>
                    <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">Start Date</label>
                    <input
                      type="date"
                      value={editingCampaign.startDate}
                      onChange={(e) => setEditingCampaign({ ...editingCampaign, startDate: e.target.value })}
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                    />
                  </div>
                  <div>
                    <label className="text-[9px] uppercase tracking-wider font-extrabold text-slate-500 block mb-1">End Date</label>
                    <input
                      type="date"
                      value={editingCampaign.endDate || ''}
                      onChange={(e) => setEditingCampaign({ ...editingCampaign, endDate: e.target.value })}
                      className="w-full h-8 px-2.5 rounded-lg border border-slate-200 bg-slate-50 text-[11px] font-medium outline-none focus:bg-white text-slate-800"
                    />
                  </div>
                </div>

                <div className="flex justify-end gap-2 pt-2 border-t border-slate-100">
                  <button
                    type="button"
                    onClick={() => setEditingCampaign(null)}
                    className="px-3.5 py-1.5 border border-slate-200 hover:bg-slate-50 text-slate-700 rounded-md text-[10px] font-bold cursor-pointer"
                  >
                    Cancel
                  </button>
                  <button
                    type="submit"
                    className="px-4 py-1.5 bg-indigo-650 hover:bg-indigo-700 text-white rounded-md text-[10px] font-bold cursor-pointer"
                  >
                    Save Changes
                  </button>
                </div>
              </form>
            </motion.div>
          </div>
        )}
      </AnimatePresence>

    </div>
  )
}
